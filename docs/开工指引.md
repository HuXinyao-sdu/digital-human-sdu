# 开工指引 V3（让历史动起来 · 任务-文件对应版）

> 用途：每个人打开就知道"这周我要写哪些文件、怎么算写完"。
> 版本：V3 · 2026-09-18

---

## 通用规则

1. 接口改了先在群里说，不私下改签名
2. 只写自己负责目录下的文件
3. 每天开始前 `git pull`，改完 commit + push
4. 每节末尾的 ✅ 是验收标准，写完才算完

---

## A：内容与AI视频

### 第1周（~09-24）

| 要写的文件 | 做什么 |
|-----------|--------|
| `data/history/events/1901-山东大学堂创办.md` | 10个事件，每篇含时间/背景/经过/意义 |
| `data/history/figures/老舍.md` 等5个 | 5个人物：老舍、侯宝璋、杨振声、赵太侔、成仿吾 |
| `data/history/buildings/柏根楼.md` 等3个 | 3栋老建筑 |
| `data/history/index.json` | 所有资料的索引清单 |

✅ 验收：`data/history/` 下至少18个md文件 + 1个index.json，每个文件模板字段完整。

### 第2周（~10-01）

| 要写的文件 | 做什么 |
|-----------|--------|
| `data/scripts/01-xxx.md` 等6篇 | 6篇口语化讲解脚本，每篇300-500字 |
| `data/scripts/index.json` | 脚本索引 |
| `data/generated-videos/01-xxx.mp4` 等6段 | EchoMimic生成讲解视频 |
| `data/photos/年代/` 下20张 | 老照片彩色化 |

✅ 验收：6段视频可播放，20张彩色照片存入对应目录。

### 第3周（~10-08）— 创新点

| 要写的文件 | 做什么 |
|-----------|--------|
| `data/scripts/dialogue-老舍-侯宝璋.md` | 3-5分钟对话脚本，按轮次标注谁说话 |
| `data/generated-videos/dialogue/laoshe-01.mp4` 等 | 老舍对话片段 |
| `data/generated-videos/dialogue/houbaozhang-01.mp4` 等 | 侯宝璋对话片段 |

✅ 验收：对话脚本自然，视频片段嘴型对应音频。

### 第4周（~10-15）

| 要写的文件 | 做什么 |
|-----------|--------|
| `data/history/` 资料校对 | 根据反馈修改错误 |
| `data/generated-videos/` 新增6段 | 扩充到12段视频 |

✅ 验收：无事实错误，共12段视频。

---

## B：全栈开发与运维

### 第1周（~09-24）

| 要写的文件 | 做什么 |
|-----------|--------|
| `backend/.env` | 配置 `RAG_API_URL` |
| `backend/app/api/chat.py` | 接入RAG真实API，替换模拟回答 |
| `frontend/src/components/AvatarViewer.vue` | **新建**：Three.js加载GLB模型组件 |
| `frontend/src/views/Guide.vue` | 接入AvatarViewer，替换emoji |
| `frontend/src/api/index.js` | 封装 `/api/chat` 调用 |

✅ 验收：
- 前端 http://localhost:5173 打开
- 后端 http://localhost:8000/docs 接口正常
- Guide页显示3D数字人（不再是emoji）
- 问答页输入问题返回RAG真实回答

### 第2周（~10-01）

| 要写的文件 | 做什么 |
|-----------|--------|
| `backend/app/api/tts.py` | **新建**：`/api/tts` 文字转语音接口 |
| `backend/app/api/content.py` | 返回真实脚本和图库数据 |
| `frontend/src/views/Timeline.vue` | 点击节点→播放讲解视频 |
| `frontend/src/views/Gallery.vue` | 展示真实照片，支持放大 |
| `frontend/src/views/Guide.vue` | 问答流程：提问→RAG→TTS→嘴动 |

✅ 验收：两种模式可切换，问答有语音，时间轴可点击播放。

### 第3周（~10-08）— 创新点

| 要写的文件 | 做什么 |
|-----------|--------|
| `backend/app/api/dialogue.py` | **新建**：`/api/dialogue` 对话脚本接口 |
| `frontend/src/components/DualAvatar.vue` | **新建**：双数字人并排组件，轮流激活 |
| `frontend/src/views/Dialogue.vue` | **新建**：跨时空对话页面 |
| `frontend/src/components/VoiceButton.vue` | **新建**："按住说话"语音按钮 |
| `frontend/src/router/index.js` | 新增 `/dialogue` 路由 |

✅ 验收：跨时空对话页可播放，双数字人同框；语音打断功能可用。

### 第4周（~10-15）

| 要写的文件 | 做什么 |
|-----------|--------|
| `frontend/src/views/Quiz.vue` | **新建**：题库嵌入页（iframe） |
| `frontend/src/router/index.js` | 新增 `/quiz` 路由 |
| 全页面UI打磨 | 按设计稿还原，修复布局 |

✅ 验收：题库可答题，UI在桌面和移动端都正常。

### 第5周（~10-22）

| 要写的文件 | 做什么 |
|-----------|--------|
| `docker-compose.yml` | 生产环境配置 |
| `deploy/nginx.conf` | Nginx反向代理 |
| 服务器部署 | 部署到10.17.51.143 |

✅ 验收：`docker compose up` 一键启动，服务器可访问。

---

## C：设计与项目管理

### 第1周（~09-24）

| 要写的文件 | 做什么 |
|-----------|--------|
| `data/avatar/avatar.glb` | Ready Player Me导出最终数字人模型 |
| `data/avatar/version-a.png` 等3张 | 3个版本形象截图，三人投票 |
| `docs/ui-design/home.png` 等4张 | 4个页面Figma设计稿 |

✅ 验收：3个版本截图 + 1个GLB模型 + 4张设计稿。

### 第2周（~10-01）

| 要写的文件 | 做什么 |
|-----------|--------|
| `docs/test-cases.md` | 15条以上测试用例 |

✅ 验收：测试用例覆盖所有核心功能。

### 第3周（~10-08）

| 要写的文件 | 做什么 |
|-----------|--------|
| `docs/user-test-plan.md` | 用户测试方案（问卷、流程） |
| 组织20-30人测试 | 找同学试用，回收问卷 |

✅ 验收：至少20份有效问卷。

### 第4周（~10-15）

| 要写的文件 | 做什么 |
|-----------|--------|
| `docs/user-test-report.md` | 测试报告（数据、问题、改进） |
| `docs/iteration-log.md` | 迭代记录（V0.1→V1.0） |

✅ 验收：报告含数据统计和问题清单。

### 第5周（~10-22）

| 要写的文件 | 做什么 |
|-----------|--------|
| `docs/final-report.md` | 结题报告初稿 |
| `docs/defense-ppt/` | 答辩PPT（15-20页） |

✅ 验收：报告完整，PPT可用于答辩。

### 第6周（~10-29）

| 要写的文件 | 做什么 |
|-----------|--------|
| 演示视频录制 | 3-5分钟项目演示视频 |
| PPT终稿 | 根据演练修改 |

✅ 验收：演示视频可播放，PPT最终版。

---

## 依赖关系速查

```
C第1周 avatar.glb ──→ B第1周 Guide.vue接入3D
C第1周 UI设计稿 ──→ B第4周 UI打磨
A第1周 history/*.md ──→ B第1周 content接口
A第2周 scripts+视频 ──→ B第2周 Timeline播放
A第3周 dialogue+视频 ──→ B第3周 Dialogue页面
B第3周 对话页完成 ──→ C第3周 用户测试
```

**最先要交的3个东西**：
1. C的数字人GLB模型
2. A的校史资料md文件
3. B的RAG接入（拿到API地址就开始）
