# 团队分工与协作规范（让历史动起来）

> 用途：团队协作的"宪法"。分工边界、接口合同、Git流程、交付标准都在这里，所有人照此执行。
> 版本：V2 · 2026-09-18（比赛版，增加创新点和外包工作）

---

## 1. 分工总表

| 成员 | 负责模块 | 交付物 | 依赖 |
|------|----------|--------|------|
| **A**（内容与AI视频） | 校史资料整理、讲解脚本撰写、跨时空对话脚本、AI讲解视频生成、老照片彩色化、RAG内容对齐 | 校史Markdown资料、讲解脚本、跨时空对话脚本、AI讲解视频、彩色照片 | 资料同学提供原始资料 |
| **B**（全栈与运维） | 前端网站、后端API、RAG接入、3D数字人、TTS接口、双数字人对话组件、语音打断、Docker部署 | 可运行的网站、双数字人对话、语音打断、部署上线 | C提供设计稿和数字人形象 |
| **C**（设计与管理） | 数字人形象设计、UI/UX设计、项目管理、测试验收、用户测试组织、结题材料、演示视频 | 数字人GLB模型、设计稿、测试报告、用户测试数据、结题材料 | — |
| **外包同学**（多人） | 资料收集、照片彩色化、用户测试、视频拍摄、宣传物料 | 原始资料、彩色照片、测试反馈、演示视频、海报 | 对应负责人对接 |

---

## 2. 两大创新点（比赛核心）

| 创新点 | 效果 | 负责人 |
|--------|------|--------|
| **多数字人跨时空对话** | 老舍和侯宝璋在屏幕上对话，讨论校史，有问有答 | A写脚本+B做前端 |
| **语音打断+多轮对话** | 数字人讲解时用户随时提问，数字人停下回答后继续 | B实现 |

---

## 3. 三条铁律

1. **边界**：每个人只在自己负责的目录/文件里写，不碰别人的。
   - A：`data/history/`、`data/scripts/`、`data/photos/`、`data/generated-videos/`、`ai/scripts/`
   - B：`frontend/`、`backend/`、`deploy/`、`docker-compose.yml`
   - C：`data/avatar/`、`docs/`、前端UI设计对接
2. **合同**：前后端之间只通过API接口交互，不直接读对方内部文件格式。数据格式以 `backend/app/models/` 里的 pydantic 定义为准。
3. **改接口必须先商量**：谁要改接口（加字段、改字段名、改返回结构），先在群里说，确认后再改；绝不默默改签名导致对端报错。

---

## 4. 目录权限对照表

| 目录 | 谁能写 | 谁能看 |
|------|--------|--------|
| `frontend/` | B | 全员 |
| `backend/` | B | 全员 |
| `data/history/` | A + 外包资料同学 | 全员 |
| `data/scripts/` | A | 全员 |
| `data/photos/` | A + 外包美术同学 | 全员 |
| `data/avatar/` | C | 全员 |
| `ai/scripts/` | A | 全员 |
| `docs/` | C（其他人也可以补充） | 全员 |
| `deploy/` | B | 全员 |

> 如果需要修改别人目录下的文件，先在群里说一声，得到同意后再改。

---

## 5. 外包工作清单

| 外包工作 | 对接人 | 交付物 | 时间节点 |
|----------|--------|--------|----------|
| 校史原始资料收集 | A | 事件/人物/建筑Markdown + 老照片 | 第2周 |
| 老照片彩色化扩充 | A | 彩色化照片存入 `data/photos/` | 第3-4周 |
| 用户测试（20-30人） | C | 问卷反馈数据 + 问题清单 | 第4周 |
| 演示视频拍摄剪辑 | C | 3-5分钟项目演示视频 | 第5周 |
| 宣传物料（海报/推文） | C | 海报、答辩PPT美化 | 第5周 |
| 校史史实校对 | A | 校对后的脚本和资料 | 第3周 |
| 题库内容扩充 | A | 校史题目录入 | 第3-4周 |

---

## 6. Git 协作流程

- **仓库**：GitHub 私有仓库 https://github.com/HuXinyao-sdu/digital-human-sdu
- **分支**：`main`（稳定分支）
- **流程**：
  1. 每天开始前：`git pull`
  2. 改完代码：`git add .` → `git commit -m "描述"`
  3. 推送前：`git pull`
  4. 推送：`git push`
- **提交规范**：`feat:` / `fix:` / `data:` / `docs:` / `refactor:` + 中文描述
- **不提交的文件**：`node_modules/`、`venv/`、`__pycache__/`、`.env`、`ai/weights/`、`.idea/`、大视频用Git LFS

---

## 7. 接口合同清单

| 接口 | 方法 | 入参 | 出参 | 负责人 |
|------|------|------|------|--------|
| `/api/health` | GET | 无 | `{status, version}` | B |
| `/api/chat` | POST | `{question, session_id?}` | `{answer, sources?, session_id}` | B（RAG对接A） |
| `/api/content/scripts` | GET | 无 | `{total, items}` | B（数据来自A） |
| `/api/content/gallery` | GET | 无 | `{total, items}` | B（数据来自A） |
| `/api/tts`（待加） | POST | `{text, voice?}` | 音频流 | B（A提供音色） |
| `/api/dialogue`（待加） | GET | 无 | `{dialogues: [{speaker, text, video_url}]}` | B（脚本来自A） |

---

## 8. 里程碑（比赛版）

| 阶段 | 时间 | A | B | C | 外包 |
|------|------|---|---|---|------|
| 阶段1 | ~09-24 | 资料收集18篇 | RAG接入+3D形象 | 形象设计+UI稿 | 资料同学收集 |
| 阶段2 | ~10-01 | 6段讲解视频 | TTS+双模式联动 | 测试用例 | 照片彩色化 |
| 阶段3 | ~10-08 | 跨时空对话脚本+视频 | 双数字人对话+语音打断 | 用户测试组织 | 20-30人试用 |
| 阶段4 | ~10-15 | 视频扩充+内容校对 | 题库嵌入+UI完善 | 反馈整理+改进 | 演示视频拍摄 |
| 阶段5 | ~10-22 | 答辩素材 | Docker上线 | 结题报告初稿 | 宣传物料 |
| 阶段6 | ~10-29 | 答辩演练 | Bug修复 | 答辩PPT+演示视频 | — |
